TZ=America/Sao_Paulo

# Beszel: preencher depois do 1º login no hub (Settings → Add system).
# APP_URL deve ser a URL pública (Tailscale serve), com o subpath /beszel.
APP_URL=http://192.168.1.100/beszel
BESZEL_KEY=
BESZEL_TOKEN=

# WUD: hash apr1 em /opt/stacks/wud/admin.hash (não versionar).
